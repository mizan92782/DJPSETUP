# ─────────────────────────────────────────────────────────────────────────────
# docker-compose.dev.yml — DEVELOPMENT STACK
# ─────────────────────────────────────────────────────────────────────────────
# Services:
#   Core:        db, redis, web, celery, celery-beat
#   Monitoring:  prometheus, grafana, loki, promtail
#
# Usage:
#   docker compose -f docker-compose.dev.yml up --build
#   docker compose -f docker-compose.dev.yml up --build -d   (detached)
#
# Dashboards:
#   API:        http://localhost:8001
#   Grafana:    http://localhost:3000   (admin / admin)
#   Prometheus: http://localhost:9090
#   Alertmgr:   http://localhost:9093
# ─────────────────────────────────────────────────────────────────────────────

services:

  # ════════════════════════════════════════════════════════════
  # CORE SERVICES
  # ════════════════════════════════════════════════════════════

  db:
    image: postgres:15-alpine
    container_name: ikon_postgres_dev
    restart: always
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-ikon_db}
      POSTGRES_USER: ${POSTGRES_USER:-ikon_user}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-ikon_pass}
    volumes:
      - postgres_data_dev:/var/lib/postgresql/data
    ports:
      - "5431:5432"           # host:container — access via localhost:5431
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-ikon_user} -d ${POSTGRES_DB:-ikon_db}"]
      interval: 5s
      timeout: 5s
      retries: 10
    networks:
      - backend

  redis:
    image: redis:7-alpine
    container_name: ikon_redis_dev
    restart: always
    command: redis-server --requirepass ${REDIS_PASSWORD:-redis_pass}
    ports:
      - "6378:6379"           # host:container
    networks:
      - backend

  web:
    build:
      context: .
      target: development
    container_name: ikon_api_dev
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - .:/app                              # live code reload
      - static_volume_dev:/app/staticfiles
      - media_volume_dev:/app/media
      - logs_dev:/app/logs                  # shared with promtail
    ports:
      - "8001:8000"
    env_file:
      - .env
    environment:
      DEBUG: "True"
      LOCAL_RUN: "False"
      DB_HOST: db
      REDIS_HOST: redis
      REDIS_PASSWORD: ${REDIS_PASSWORD:-redis_pass}
      DJANGO_SETTINGS_MODULE: project.settings
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_started
    networks:
      - backend
      - monitoring

  celery:
    build:
      context: .
      target: development
    container_name: ikon_celery_dev
    command: celery -A project worker --loglevel=info --concurrency=2 --logfile=/app/logs/celery_worker.log
    volumes:
      - .:/app
      - logs_dev:/app/logs
    env_file:
      - .env
    environment:
      DEBUG: "True"
      LOCAL_RUN: "False"
      DB_HOST: db
      REDIS_HOST: redis
      REDIS_PASSWORD: ${REDIS_PASSWORD:-redis_pass}
      DJANGO_SETTINGS_MODULE: project.settings
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_started
    networks:
      - backend

  celery-beat:
    build:
      context: .
      target: development
    container_name: ikon_celery_beat_dev
    command: celery -A project beat --loglevel=info --scheduler django_celery_beat.schedulers:DatabaseScheduler --logfile=/app/logs/celery_beat.log
    volumes:
      - .:/app
      - logs_dev:/app/logs
    env_file:
      - .env
    environment:
      DEBUG: "True"
      LOCAL_RUN: "False"
      DB_HOST: db
      REDIS_HOST: redis
      REDIS_PASSWORD: ${REDIS_PASSWORD:-redis_pass}
      DJANGO_SETTINGS_MODULE: project.settings
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_started
      celery:
        condition: service_started
    networks:
      - backend

  # ════════════════════════════════════════════════════════════
  # PROMETHEUS — Metrics Scraping
  # ════════════════════════════════════════════════════════════

  prometheus:
    image: prom/prometheus:latest
    container_name: ikon_prometheus_dev
    restart: unless-stopped
    volumes:
      - ./infra/prometheus/prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - prometheus_data_dev:/prometheus
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
      - '--storage.tsdb.path=/prometheus'
      - '--storage.tsdb.retention.time=7d'      # shorter retention in dev
      - '--web.enable-lifecycle'
    ports:
      - "9090:9090"
    networks:
      - monitoring

  # ════════════════════════════════════════════════════════════
  # LOKI — Log Aggregation
  # ════════════════════════════════════════════════════════════

  loki:
    image: grafana/loki:latest
    container_name: ikon_loki_dev
    restart: unless-stopped
    volumes:
      - ./infra/loki/loki-config.yml:/etc/loki/local-config.yaml:ro
      - loki_data_dev:/loki
    command: -config.file=/etc/loki/local-config.yaml
    ports:
      - "3100:3100"
    networks:
      - monitoring

  # ════════════════════════════════════════════════════════════
  # PROMTAIL — Log Shipping Agent
  # ════════════════════════════════════════════════════════════

  promtail:
    image: grafana/promtail:latest
    container_name: ikon_promtail_dev
    restart: unless-stopped
    volumes:
      - ./infra/promotail/promtail-config.yml:/etc/promtail/config.yml:ro
      - logs_dev:/app/logs:ro               # reads Django/Celery logs
      - /var/run/docker.sock:/var/run/docker.sock:ro
    command: -config.file=/etc/promtail/config.yml
    depends_on:
      - loki
    networks:
      - monitoring

  # ════════════════════════════════════════════════════════════
  # ALERTMANAGER — Alert Routing (optional in dev)
  # ════════════════════════════════════════════════════════════

  alertmanager:
    image: prom/alertmanager:latest
    container_name: ikon_alertmanager_dev
    restart: unless-stopped
    entrypoint: ["/bin/sh", "/etc/alertmanager/entrypoint.sh"]
    env_file:
      - .env
    volumes:
      - ./infra/alratmanager/alertmanager.yml.template:/etc/alertmanager/alertmanager.yml.template:ro
      - ./infra/alratmanager/templates:/etc/alertmanager/templates:ro
      - ./infra/alratmanager/entrypoint.sh:/etc/alertmanager/entrypoint.sh:ro
    ports:
      - "9093:9093"
    networks:
      - monitoring

  # ════════════════════════════════════════════════════════════
  # GRAFANA — Visualization Dashboard
  # ════════════════════════════════════════════════════════════

  grafana:
    image: grafana/grafana:latest
    container_name: ikon_grafana_dev
    restart: unless-stopped
    environment:
      GF_SECURITY_ADMIN_USER: ${GRAFANA_ADMIN_USER:-admin}
      GF_SECURITY_ADMIN_PASSWORD: ${GRAFANA_ADMIN_PASSWORD:-admin}
      GF_USERS_ALLOW_SIGN_UP: "false"
      GF_AUTH_ANONYMOUS_ENABLED: "true"         # allow read-only access in dev
      GF_AUTH_ANONYMOUS_ORG_ROLE: "Viewer"
    volumes:
      - ./infra/grafana/provisioning:/etc/grafana/provisioning:ro
      - grafana_data_dev:/var/lib/grafana
    ports:
      - "3000:3000"
    depends_on:
      - prometheus
      - loki
    networks:
      - monitoring

# ════════════════════════════════════════════════════════════
# NETWORKS
# ════════════════════════════════════════════════════════════

networks:
  backend:
    driver: bridge
  monitoring:
    driver: bridge

# ════════════════════════════════════════════════════════════
# VOLUMES
# ════════════════════════════════════════════════════════════

volumes:
  postgres_data_dev:
  static_volume_dev:
  media_volume_dev:
  logs_dev:               # shared between web, celery, celery-beat, promtail
  prometheus_data_dev:
  loki_data_dev:
  grafana_data_dev:
