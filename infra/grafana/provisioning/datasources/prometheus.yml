# infra/grafana/provisioning/datasources/prometheus.yml
# Grafana Datasource Provisioning — Prometheus (Metrics)
apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    jsonData:
      timeInterval: "15s"
      httpMethod: POST
    version: 1
    editable: true
